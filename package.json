{
  "name": "portfolio-monorepo",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "npx lerna run dev",
    "build": "cd packages/strapi && yarn run build",
    "start": " cd packages/strapi && yarn run start"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
